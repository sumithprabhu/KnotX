"use client"

import React from "react"

// This component will wrap the app with wallet providers
// For now, it's a placeholder - actual implementation will be added when packages are installed

export function WalletProviders({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}

